import 'package:flutter/material.dart';

import 'breathing_exercise_screen.dart';
import 'breathing_technique.dart';

class Relaxation478Screen extends StatelessWidget {
  const Relaxation478Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return BreathingExerciseScreen(technique: k478Relaxation);
  }
}
